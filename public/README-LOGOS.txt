SAVE THE LOGO FILES HERE
========================

Save four TRANSPARENT-BACKGROUND PNGs in this /public folder, using these
EXACT filenames (the code already points at them):

  1. logo-wordmark.png         Full "The Corporate Baklawa" lockup,
                               ORIGINAL colors (dark green + gold).
                               → for light backgrounds.

  2. logo-wordmark-ivory.png   Same wordmark, IVORY / WHITE version.
                               → used in the FOOTER (charcoal background).

  3. logo-monogram.png         TCB monogram, ORIGINAL colors.
                               → used as the browser-tab FAVICON.

  4. logo-monogram-ivory.png   TCB monogram, IVORY / WHITE version.
                               → used in the NAVBAR (over the hero & when scrolled).

IMPORTANT
---------
* Backgrounds MUST be transparent. The versions shared in chat have a solid
  ivory fill — that would show as a box on any colored background.
* Until a file is added, the navbar/footer fall back to the brand name in text,
  so the site never shows a broken image.
* Export at ~2x display size for crisp retina rendering
  (wordmark ≈ 480px wide, monogram ≈ 200px wide).

Prefer hosting on ImageKit instead? Just put the full URLs into
lib/data.ts → site.logo / logoWhite / monogram / monogramWhite.
